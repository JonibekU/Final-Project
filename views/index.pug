extends layout

block content
  section.hero
    div.hero-content
      h2 Fresh Donuts Since 1992
      p Serving the community with warm, delicious donuts and coffee every day.

      div.hero-buttons
        a.btn(href="#") View Menu
        a.btn.secondary(href="#") Order Online